using System;
using System.Collections.Generic;
using System.Linq;
using System.Runtime.InteropServices;
using System.Text;
using System.Threading.Tasks;

namespace Win3muCore
{
    /*
    public static class MarshalStruct
    {
        public static T FromPointer<T>(IntPtr ptr) where T : struct
        {
            return (T)Marshal.PtrToStructure(ptr, typeof(T));
        }

        public static void ToPointer<T>(T t, IntPtr ptr, bool deleteOld) where T : struct
        {
            Marshal.StructureToPtr(t, ptr, true);
        }
    }
    */
}
